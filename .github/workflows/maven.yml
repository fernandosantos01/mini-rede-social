# This workflow will build a Java project with Maven, and cache/restore any dependencies
# to improve the workflow execution time.
# For more information see:
# https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout do repositório
      - uses: actions/checkout@v4

      # Configuração do JDK 21 (compatível com o Maven Compiler Plugin configurado como release 21)
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      # Compila e empacota o projeto com Maven
      - name: Build with Maven
        run: mvn -B clean package --file pom.xml

      # Executa os testes automatizados
      - name: Run Tests
        run: mvn test

      # (Opcional) Gera e envia o grafo de dependências para o Dependabot
      - name: Update dependency graph
        uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
